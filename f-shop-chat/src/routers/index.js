import React from 'react';

const publicRouters = [
    {
        name: "Login",
        path: "/"
    }
]

const privateRouters = [
    {
        name: "Login",
        path: "/"
    }
]
export const ReactComponent = () => {
    return <div>a</div>
}